# landingPage

React + Vite landing page project.

## Getting started
- Install dependencies: npm install
- Run dev server: npm run dev
- Build for production: npm run build

## Tech stack
- React 18 + Vite
- ESLint configured via eslint.config.js
